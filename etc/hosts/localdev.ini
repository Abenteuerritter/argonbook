# Local Development Environment
# =============================
#
# This inventory uses the vagrant machine as target host. To configure it run
# in the project's root directory::
#
# vagrant ssh-config >> ~/.ssh/config

[dev]
argonbook.local ansible_ssh_user=vagrant ansible_become=yes

[db:children]
dev

[web:children]
dev

[all:vars]
packages            = ['git']
domain              = argonbook.local
working_dir         = /home/vagrant/argonbook
php_memory_limit    = 392M
db_buffer_pool_size = 392M
db                  = argonbook
