{% trans_default_domain "ArgonNewsBundle" %}
<article>
  <h4>{{ post.title }}</h4>
  <main>
    {{ post.body|nl2br }}
  </main>
  <footer>
    {{ "admin.news.post.footer"|trans({"%creator%": post.creator, "%published%": post.publishedAt|date}) }}
  </footer>
</article>
