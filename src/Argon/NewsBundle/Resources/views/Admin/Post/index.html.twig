{% extends "ArgonWebBundle::layout_admin.html.twig" %}
{% set active = "news" %}

{% block title %}{{ parent() }} - {{ "admin.news.news"|trans }}{% endblock %}

{% block top %}
<nav aria-label="{{ "admin.you_are_here"|trans }}" role="navigation">
  <ul class="breadcrumbs">
    <li><a href="{{ path("admin_dashboard") }}">{{ "admin.dashboard.title"|trans }}</a></li>
    <li>{{ "admin.news.news"|trans }}</li>
  </ul>
</nav>
{% endblock %}

{% block content %}
<table>
  <thead>
    <tr>
      <th>{{ "admin.news.title"|trans }}</th>
      <th>{{ "admin.news.status"|trans }}</th>
      <th>&nbsp;</th>
    </tr>
  </thead>
  <tbody>
  {% for post in entities %}
    <tr>
      <td>
        <a href="{{ path("admin_news_view", {"slug": post.slug}) }}">{{ post }}</a>
      </td>
      <td>
        {% if post.isPublished %}
          {{ "admin.news.published_at"|trans({"%date%": post.publishedAt|date}) }}
        {% else %}
          {{ "admin.news.draft"|trans }}
        {% endif %}
      </td>
      <td>
        <div class="button-group">
          {% if not post.isPublished %}
            <a href="{{ path("admin_news_publish", {"slug": post.slug}) }}" class="button">{{ "admin.news.publish"|trans }}</a>
          {% endif %}
          <a href="{{ path("admin_news_edit", {"slug": post.slug}) }}" class="button">{{ "admin.news.edit"|trans }}</a>
        </div>
      </td>
    </tr>
  {% else %}
    <tr class="warning">
      <td colspan="3">{{ "admin.news.empty"|trans }}</td>
    </tr>
  {% endfor %}
  </tbody>
</table>
<div class="button-group">
  <a href="{{ path("admin_news_new") }}" class="button">{{ "admin.news.new"|trans }}</a>
</div>
{% endblock %}